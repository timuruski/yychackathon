<%= form_for(@interesting_tweet) do |f| %>
  <% if @interesting_tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interesting_tweet.errors.count, "error") %> prohibited this interesting_tweet from being saved:</h2>

      <ul>
      <% @interesting_tweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

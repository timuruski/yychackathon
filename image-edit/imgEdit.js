

var Fluffy = Fluffy || {}; // setup namespace

Fluffy.ImgEdit = function() // setup module 
{
    var inputCanvas;
    var outputCanvas;

    function init()
    {
    }

    var publicExport =
        {
         
            init: init
        };

    return publicExport;
}();

Fluffy.ImgEdit.init();
